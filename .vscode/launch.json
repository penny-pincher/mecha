{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug application",
      "cargo": {
        "args": ["build", "--bin=mecha", "--package=mecha"],
        "filter": {
          "name": "mecha",
          "kind": "bin"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
